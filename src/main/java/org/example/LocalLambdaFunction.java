package org.example;

public class LocalLambdaFunction {
    public int processUserLogin(int loginCount) {
        // Perform operations here: double the login count
        int doubledLoginCount = loginCount * 2;
        // Return the doubled value
        return doubledLoginCount;
    }
}